python-dotenv==1.0.1
flask>=3.0.0
requests==2.32.3
beautifulsoup4>=4.12.0
lxml>=4.9.0
html2text>=2020.1.16
playwright>=1.40.0
numpy<2.0.0
faster-whisper==1.0.3
setuptools<81
sounddevice==0.4.7
pytesseract==0.3.13
Pillow==10.4.0
webrtcvad==2.0.10
rapidfuzz==3.6.1
pynput>=1.7.6
# Mouse/keyboard control for the computerUse tool. Needs a desktop
# session; the tool imports it lazily so headless installs still work.
pyautogui>=0.9.54
geoip2==4.8.0
tzdata==2026.1; sys_platform == "win32"
miniupnpc==2.2.8
pytest==8.3.2
pytest-repeat==0.9.3
# mcp 2.x removes the initialize handshake and reworks the client
# surface; the exact pin keeps installs on the 1.x contract this
# codebase targets.
mcp==1.13.1
# Local OS credential store (macOS Keychain, Windows Credential
# Manager, Linux Secret Service) for API keys. Jarvis degrades to
# config.json when no backend is present.
keyring==25.7.0
# Chatterbox TTS is optional and lives in requirements-chatterbox.txt.
# It pulls torch and the CUDA stack, which is ~4 GB nobody using the
# default Piper voice should have to download.
piper-tts>=1.3.0
pygame>=2.1.0
faiss-cpu>=1.7.4

# NVIDIA CUDA libraries for GPU-accelerated speech recognition on Windows
nvidia-cublas-cu12>=12.8.0; sys_platform == "win32"
nvidia-cudnn-cu12>=9.0.0; sys_platform == "win32"

# MLX Whisper for Apple Silicon Macs (much faster than CPU-based faster-whisper)
mlx-whisper>=0.4.0; sys_platform == "darwin" and platform_machine == "arm64"

# Desktop app dependencies
PyQt6>=6.6.0
PyQt6-WebEngine>=6.6.0
psutil>=5.9.0
# Note: 6.13.0+ has VC runtime pre-loading fix for Windows
pyinstaller>=6.13.0

# Acoustic echo cancellation, so the microphone can be kept open while
# Jarvis speaks. Genuinely optional — `create_echo_canceller` catches the
# ImportError and falls back to passing microphone audio through untouched,
# and the feature is off by default. Listing it unconditionally would make
# `pip install -r requirements.txt` fail outright on any platform pyaec has
# no wheel for, so it lives in requirements-aec.txt instead.
